{% extends 'base.html.twig' %}

{% block title %}Modifier un article{% endblock %}

{% block body %}
    <div class="container mt-5" data-controller="edit-article"
         data-edit-article-article-id-value="{{ article.id }}">
        <h1>Modifier un article</h1>

        <form id="article-form" data-action="submit->edit-article#submit">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input type="text" id="title" data-edit-article-target="title" 
                       class="form-control" value="{{ article.title }}" required>
            </div>

            <div class="mb-3">
                <label for="description" class="form-label">Description</label>
                <textarea id="description" data-edit-article-target="description" 
                          class="form-control" required>{{ article.description }}</textarea>
            </div>

            <div class="mb-3">
                <label for="body" class="form-label">Body</label>
                <textarea id="body" data-edit-article-target="body" 
                          class="form-control" rows="5" required>{{ article.body }}</textarea>
            </div>

            <button type="submit" class="btn btn-primary"> Modifier </button>
        </form>

        <div id="form-response" data-edit-article-target="response" class="mt-3"></div>

        <div data-controller="editArticle">
            <input type="hidden" data-edit-article-target="id" value="{{ article.id }}">
        </div>
    </div>
{% endblock %}
