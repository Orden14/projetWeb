{# templates/article/new.html.twig #}

{% block title %}Create New Article{% endblock %}

{% block body %}
    <h1>Create a New Article</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">{{ form.save.vars.label }}</button>
    {{ form_end(form) }}
{% endblock %}
